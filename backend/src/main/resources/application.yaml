spring:
  application:
    name: ticketing_app
  datasource:
    url: jdbc:mysql://localhost:3306/ticketing_app_db
    username: root
    password: mysql
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect
